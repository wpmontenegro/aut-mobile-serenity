ios.local {
  include "../types/local.conf"

  appium {
    include "../platforms/ios.conf"

    deviceName = iPhone 14 Pro Max
    platformVersion = "17.0"
    app = "classpath:app/MyRNDemoApp.zip"
  }
}