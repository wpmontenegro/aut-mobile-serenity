serenity {
  project.name = Automatización Mobile Android/iOS
  # Pause between each step in miliseconds
  step.delay = 800
  take.screenshots = AFTER_EACH_STEP
  # Nivel de registro de trazas
  logging = VERBOSE
}

webdriver {
  driver = appium
  # Implicit wait in miliseconds
  timeouts.implicitlywait = 40000
}

appium {
  hub = "http://0.0.0.0:4723"
  console = true
  autoGrantPermissions = true
  autoAcceptAlerts = true
}

environment = ${?platform}

environments {
  android {
    appium {
      platformName = android
      automationName = UIAutomator2
      deviceName = emulator-5554
      platformVersion = "13.0"
      appPackage = com.saucelabs.mydemoapp.rn
      appActivity = com.saucelabs.mydemoapp.rn.MainActivity
    }
  }
  ios {
    appium {
      platformName = ios
      automationName = XCUITest
      deviceName = iPhone 14 Pro Max
      platformVersion = "17.0"
      bundleId = com.saucelabs.mydemoapp.rn
      app = "/Users/wmontene/Downloads/Proyectos/aut-mobile-serenity/src/test/resources/app/MyRNDemoApp.zip"
    }
  }
}

# Caracteristicas del reporte
enable.markdown = "story,narrative,scenario,step"